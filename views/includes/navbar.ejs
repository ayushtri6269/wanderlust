<nav class="navbar navbar-expand bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="/listings" aria-label="WanderLust home">
      <i class="fa-solid fa-compass" aria-hidden="true"></i>
      <span class="brand-text ms-2">WanderLust</span>
    </a>

    <div class="navbar-collapse d-flex align-items-center" id="navbarNavAltMarkup">
      <ul class="navbar-nav me-auto mb-0">
        <li class="nav-item"><a class="nav-link" href="/listings">Explore</a></li>
        <li class="nav-item"><a class="nav-link" href="/listings/new">Add New Listing</a></li>
      </ul>

      <form class="d-flex my-0 nav-search" action="/listings" role="search" aria-label="Search listings">
        <input class="form-control me-2 search-input" name="q" type="search" placeholder="Search" aria-label="Search Destinations" value="<%= typeof q !== 'undefined' ? q : '' %>">
        <button class="btn btn-search" type="submit" aria-label="Submit search"><i class="fa-solid fa-magnifying-glass" aria-hidden="true"></i></button>
      </form>

      <ul class="navbar-nav ms-auto mb-0 d-flex align-items-center">
        <!-- Dark Mode Toggle -->
        <li class="nav-item me-3">
          <div class="form-check form-switch mb-0">
            <input class="form-check-input" type="checkbox" id="darkModeToggle" aria-label="Toggle dark mode">
            <label class="form-check-label" for="darkModeToggle">
              <i class="fa-solid fa-moon"></i>
            </label>
          </div>
        </li>

        <% if (typeof currentUser === 'undefined' || !currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/signup"><strong>Signup</strong></a></li>
          <li class="nav-item"><a class="nav-link" href="/login"><strong>Login</strong></a></li>
        <% } else { %>
          <li class="nav-item dropdown">
            <a class="nav-link d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <div class="user-avatar"><%= currentUser.username.charAt(0).toUpperCase() %></div>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><h6 class="dropdown-header"><%= currentUser.username %></h6></li>
              <li><span class="dropdown-item-text small text-muted"><%= currentUser.email %></span></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/logout">Logout</a></li>
            </ul>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
